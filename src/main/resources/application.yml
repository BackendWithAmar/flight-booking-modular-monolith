spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flight_system
    username: postgres
    password: Goloo$76652

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true